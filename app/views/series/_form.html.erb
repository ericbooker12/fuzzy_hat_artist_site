<%= form_for(series) do |f| %>
  <% if series.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(series.errors.count, "error") %> prohibited this series from being saved:</h2>

      <ul>
      <% series.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :archive %>
    <%= f.check_box :archive %>
  </div>

  <div class="field">
    <%= f.label :thumbnail %>
    <%= f.number_field :thumbnail %>
  </div>

  <div class="field">
    <%= f.label :gallery_id %>
    <%= f.text_field :gallery_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
