<p id="notice"><%= notice %></p>

<h1>Images of a series</h1>

<ul>
  <% @items.each do |item|   %>
  <li>
    <%= link_to 'Photo placeholder', collection_item_path(item.collection_id, item)  %>
    <%= item.title %>
  </li>
  <% end %>
</ul>
