<% if @archived_galleries %>
  <h1>** Archived **</h1>
    <ul>
      <% @archived_galleries.each do |gallery| %>
      <li>
        <%= gallery.name  %>
        <%= link_to 'edit', edit_gallery_path(gallery) %>

      </li>
      <% end %>
    </ul>

<% else %>
  <h1>There aren't any archived galleries.</h1>
<% end %>
<%= link_to 'Back', galleries_path %>
