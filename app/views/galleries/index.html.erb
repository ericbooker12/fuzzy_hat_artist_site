<h1>Mark Einert</h1>

<% @galleries.each do |gallery|  %>
  <ul id="main-galleries">
    <li>
      <%= link_to gallery.name, gallery_groups_path(gallery.id), title: 'gallery' %>
    </li>
  </ul>
<% end %>
