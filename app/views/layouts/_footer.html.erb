<footer class="footer navbar-fixed-bottom">
	<% if !user_signed_in? %>
		<small>
			<class="text-muted"><%= link_to "Contact the Artist ", '#', id: "contact-artist" %> |
			<%= link_to "Back to #{@gallery.name} collections", gallery_collections_path(@collection.gallery_id), class: 'text-muted' %>
		</small>
	<% end %>
</footer>
